import { ids } from "webpack"

let sum = 0
const products = [
    54,23,542,123
]

products.forEach((el, index)=>{
    console.log(`Index: ${index} value: ${el}`)
    sum+=el
})
console.log(sum)

<tag atributes>...</tag>
id class placeholde width background color